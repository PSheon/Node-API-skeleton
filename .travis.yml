dist: bionic
language: node_js
node_js:
  - '10'
  - '12'
cache:
  npm: true
services:
  - mongodb
before_script:
  - cp .env.example .env
env:
  - NODE_ENV=test
deploy:
  provider: heroku
  api_key: $HEROKU_DEPLOY_TOKEN
  app: node-api-skeleton
  on:
    branch: master
    tags: true
